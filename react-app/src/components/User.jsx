import React from 'react';

const User = (props) => {
    return (
        <div className={"user"}>
            <h4>{props.user.name}</h4>
            <div>

            </div>

        </div>
    );
};

export default User;